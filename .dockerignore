# Keep the build context small and secrets out of images.
.venv/
.git/
node_modules/
frontend/node_modules/
frontend/dist/
data/corpus/
data/eval_runs/
.env
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
# NOTE: .chroma/ and data/bm25_*.pkl are deliberately NOT ignored — the
# Dockerfile copies the prebuilt indexes into the image (see comment there).
