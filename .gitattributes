# Normalize line endings: text files stored as LF in git.
# Prevents the classic Windows->Linux container failure where a CRLF shell
# script dies with ": command not found".
* text=auto
*.sh text eol=lf
*.py text
*.md text
Dockerfile text eol=lf
